import React from "react";

const TodoInput = () => {
  return (
    <div>
      <h3>Add new Todo</h3>
      <input type="text" placeholder="Enter the title for new todo" />
      <button>Add</button>
    </div>
  );
};

export default TodoInput;
